// import { render, screen, within } from "@testing-library/react";
// import PreviewPanel from "./PreviewPanel";

// test("shows empty preview when nothing is selected", () => {
//   render(<PreviewPanel selectedComponent={null} />);
//   const stage = screen.getByTestId("preview-stage");
//   expect(within(stage).getByTestId("preview-empty")).toBeInTheDocument();
// });

// test("mounts a preview container when selected", () => {
//   render(<PreviewPanel selectedComponent="AnyKey" />);
//   const stage = screen.getByTestId("preview-stage");
//   expect(within(stage).getByTestId("preview-mounted")).toBeInTheDocument();
// });
