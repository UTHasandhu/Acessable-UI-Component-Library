import React from "react";

export default function Preview() {
    return (
        <div className="flex items-center justify-center h-full bg-gray-50 text-gray-500">
            <p>Preview Area (Component will render here)</p>
        </div>
    );
}